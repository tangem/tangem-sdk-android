ext.artifactConfig = [
        group: project.hasProperty('artifactGroup') ? project.getProperty('artifactGroup') : 'com.github.Tangem',
        version: project.hasProperty('artifactVersion') ? project.getProperty('artifactVersion') : '0.0.1'
]